// SLIM: removed — must return {} (not undefined) because callers index into the result
export type parseFrontmatterBlock = any;
export function parseFrontmatterBlock(..._args: any[]): any { return {}; }
