{
  "name": "@octopus/quota",
  "version": "1.0.0",
  "description": "配额管理模块",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "test": "vitest",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "ioredis": "^5.3.0"
  }
}
